# 鱼骨图版本记录

## V2.0（2026-09-08）定稿版本

- 当前版本：鱼骨图 V2.0（定稿），V1.0/V1.1/V1.2 历史节不回改
- 定稿范围：现有 Vue3 编辑器、只读预览、导入导出与 V1.x 全部能力保持稳定

### V2.0 新增/收口

- 文档列表“新建鱼骨图”不再使用浏览器 prompt，改为列表页内弹窗
- 弹窗包含：鱼骨图名称输入、模板平铺卡片（空骨架 / 经典六原因 / 根因分析）、7 套鱼骨预设样式预览
- 模板与样式预览组件只负责界面示意，不修改后端数据与创建业务逻辑
- 选择模板与样式后创建文档，自动带入对应模板与样式进入编辑器
- 预览组件鱼干按鱼头/鱼尾实际包围盒动态对齐；鱼刺按最短等长绘制
- UI 方向暂以现有 Vue 编辑器风格为基础，Figma 风格整体换肤未纳入本次定稿

### 回归

- `vue-create-dialog.js`：弹窗模板/样式数量、创建后标题/预设/节点数验证
- `vue-smoke.js`、`vue-preview-readonly.js` 等既有回归同步通过，均为 `errors: []`

## V1.2（2026-09-07）独立只读预览界面

- 当前版本：鱼骨图 V1.2，在 V1.0/V1.1 基线上新增；前序基线节不回改

### 已实现

- 新增独立只读预览入口：URL 使用 `?docId=xxx&view=preview`（兼容 `preview=1` / `mode=preview`）
- 预览页全屏只展示鱼骨图画布，无顶部工具栏、左侧组件库、右侧属性面板与层级弹窗
- 保留左上角缩放控件与 PNG / SVG / PDF 导出按钮
- 支持按住空格 + 鼠标左键拖动平移（单击不拖动、不锁定），触屏单指平移，缩放与导出不受影响
- 只读保护：节点/连线不可拖拽、不可双击编辑、右键菜单与快捷键编辑均禁用，端口/连接点/选择框隐藏
- 文档列表新增“预览”按钮
- 原编辑器 `view=preview` 外路径保持原有全部功能，仅通过 `readonly: true` 分支启用只读逻辑

### 回归

- 新增 `tests/vue-preview-readonly.js`：断言无工具栏/组件库/属性面板、缩放导出可用、节点位置不变、双击不弹编辑、拖动平移生效、导出菜单为 PNG/SVG/PDF，均为 `errors: []`
- 既有回归 vue-smoke / vue-preset7-style / vue-hierarchy-noop / vue-text-fit-ellipsis 同步通过

## V1.1（2026-09-04）文字边框省略号与多行编辑

- 当前版本：鱼骨图 V1.1，在 V1.0 基线上新增；V1.0 基线节不回改
- 基线依据：`docs/HANDOFF.md` 第 1 至 20 节

### 已实现

- 一级/二级鱼刺及原因分组文字按节点宽高减内边距折行，固定框体不随文字自动放大
- 超出框体行数时按框截断并显示省略号；模型 `label/text` 保留完整原文，再次编辑或导出不丢内容
- 拖动节点框或属性面板改宽高后，显示文字按新尺寸即时重算，省略号随框体出现/消失
- 双击编辑改为多行文本域：Enter 换行，Ctrl/Cmd+Enter 或失焦提交，Esc 取消
- 属性面板文本输入改为 textarea，可保留换行
- 历史文档/保存后重载时，载入完成后即按框重算并显示省略号，无需先进入编辑；载入阶段静默处理，不会把文档误标为已修改
- 根目录 `js/*` 与 `vue/public/js/*` 同步，`vue/dist` 已重建

### 回归

- 新增 `tests/vue-text-fit-ellipsis.js` 与 `tests/vue-text-load-ellipsis.js`：覆盖长文本省略、放大显示全文、缩回重新省略、Enter 换行与 Ctrl+Enter 提交，均为 `errors: []`

## V1.0（2026-09-04）当前版本基线

- 基线提交：`v1.0`，分支 `main`，已同步 GitHub `origin/main`
- 稳定快照：`.rollback/stable-line-refresh-20260903`
- 本轮改动快照：`.rollback/pre-edge-refresh-converge-20260903`
- 基线依据：`docs/HANDOFF.md` 第 1 至 19 节
- 用途：作为鱼骨图 V1.0 功能基线；后续版本只在本文件新增版本节，不回改本节

### 技术架构

- Vue 3 + Vite 前端，AntV X6 3.1.8 画布引擎，jsPDF 本地离线资源
- FastAPI 后端，JWT Bearer 鉴权，SQLite / PostgreSQL / Oracle 存储
- 前端接口地址由 `vue/dist/config.js` 的 `window.YGT_API_BASE` 配置
- 构建产物支持根路径与 `/vue/ygt/` 子路径部署

### 图形与画布

- 鱼骨骨架：鱼头、鱼尾、鱼干/中脊
- 业务节点：一级鱼刺 group、二级及以下 bone、原因分组、文本框、图片、连接点磁点
- 7 套鱼形预设，支持左右朝向切换
- 模板：空骨架、经典六原因、根因分析
- 图片背景、图片节点、超链接

### 编辑与交互

- 组件库拖拽、节点连线、双击编辑文字
- 属性面板：颜色、字号、字重、填充、边框、线宽、透明度
- 批量样式、对齐与分布
- Undo / Redo、历史面板、复制、剪切、粘贴、快速复制、删除、清空
- 空格平移、方向键微调、Ctrl+A 全选、Ctrl+滚轮缩放
- 拖拽吸附、右键菜单、快捷键说明浮层
- 通用鱼刺设置：按层级设置线条与节点控件并随文档保存
- 鱼头文字默认跟随填充色，支持横向 / 竖向文字
- 自研取色弹层：色块、Hex、RGB、确认后应用

### 端口、连线与箭头

- 四向端口：left / right / top / bottom
- 端口自动重锚与手动方向覆盖，加载时自动归一化
- `bone-edge` 直线连线，支持节点到节点、节点到线体、线体到线体
- 连线起点、终点可拖拽，支持吸附线段、吸附端口、拖成自由点
- Alt+单击线体添加连接点，双击鱼干添加连接点
- 预览 / 导出时隐藏连接点并补齐线段空隙
- 箭头按线宽精确贴端：`refX = -(线宽 + 5.5)`

### 层级、同步与自动修复

- 业务节点记录 `parentId / order / level`，kind 为 head / group / bone
- 校验：父级存在、无环、根节点存在、深度限制、同级排序连续
- 保存与加载按连线同步层级，连线优先于旧 `data.parentId`
- 节点拖到目标端口松手改父级，成环或超限拦截
- 悬空连线自动回连到父级线体，并按排序分配锚点比例
- 加载后对 edge-to-edge 依赖链按父线先于子线收敛刷新视图

### 层级数据编辑

- 层级树弹窗：新增一级、新增子级、新增同级、删除、上移、下移、拖拽调级
- 结构操作即时应用到画布并写入一步历史
- 名称与重要标志保存时应用，不做结构性改动时不改写坐标与锚点

### 后端与存储

- 文档 CRUD：列表、新建、读取、保存、重命名、删除
- 主表 `hl_ygt` 存完整文档 JSON 与鱼头标题，明细表 `hl_ygtmx` 存业务层级
- 保存按线段解析父级，不依赖前端伪造的 `parentId`
- `positions` 为空时按 `hl_ygtmx` 自动重建布局
- token 开发脚本、日志滚动、配置示例与部署脚本齐备

### 导出与产物

- 导出 PNG、SVG、PDF、JSON，导入 JSON
- 画布左上角导出菜单与缩放控件
- 构建命令：`build.bat`、`build_frontend.bat`、`build_exe.bat`、`package.bat`

### 测试与工程化

- Playwright 页面级回归与后端数据校验脚本
- V1.0 全量 12 项前端回归、悬空回连回归、edge-to-edge 刷新回归通过，均为 `errors: []`
- Git 已接入：`main` 对应远端，回滚快照保留在 `.rollback/`

### V1.0 已知边界

- 箭头线拖入自动吸附成真连线：已决定暂不实现，方案保留在 `docs/待考虑方案.md`
- 层级树 UI 允许新增到数据第 5 级，但 `validateHierarchy` 会把鱼头计入深度，边界口径待统一
- 更多行业模板、多文档合并、撤销历史持久化、真机触屏回归尚未列入 V1.0
